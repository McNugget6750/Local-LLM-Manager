# Python
.venv/
__pycache__/
*.pyc
*.pyo

# Local machine config — contains absolute paths with username
commands.json
eli.toml
config.json

# Eli's runtime state — operational, not config
MEMORY.md
.chat_history
sessions/
llama.log
*.log

# Personal user profile
USER_PROFILE.md

# Telegram bot secrets and runtime state
telegram_bot/.env
telegram_bot/bot_auth.db*
telegram_bot/blocklist.txt

# Local GUI preferences
ui_prefs.json

# Scheduler state — contains user Telegram IDs
schedules.json

# Artifacts
render_power_curve.py
audio.raw
audio.wav
audio_full.wav
audio_stream.wav

# Kokoro model files — large binaries, download separately
kokoro_models/
